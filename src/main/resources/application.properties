# Server
server.port=8090
spring.application.name=FileNode

# Datasource (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/cloudbox
spring.datasource.username=cloudbox_user
spring.datasource.password=cloudbox123

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# File storage
storage.location=/Users/antonabitharshan/Desktop/storage

# JWT configuration
jwt.secret=CloudBoxSuperSecretKeyWithAtLeast32Chars!123
jwt.expiration=86400000

# File upload limits
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB